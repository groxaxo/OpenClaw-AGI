# Default OpenClaw-RL requirements
# ============================
# The default pipeline is the lightweight Unsloth Qwen 3.5 (4B) Vision path
# tuned for 2x/3x RTX 3090-class GPUs.
#
# For the deprecated Slime/Megatron 8x-GPU research stack, install:
#   pip install -r requirements-legacy.txt
-r requirements-qlora.txt
